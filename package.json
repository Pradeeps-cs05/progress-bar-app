{
  "name": "progress-bar-app",
  "version": "1.0.0",
  "main": "server/server.js",
  "scripts": {
    "start": "cd client && npm install && npm run build && node server/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0"
  },
  "engines": {
    "node": "20.x"
  }
}